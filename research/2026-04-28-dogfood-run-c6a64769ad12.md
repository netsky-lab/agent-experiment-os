# Dogfood Run: Experiment OS Backlog

Date: 2026-04-28

Run id: `run.c6a64769ad12`

## Task

Implement the backlog spanning matrix evidence, MCP completion contracts, issue ingestion, frontend, policy gates, and dogfood reporting.

## Observation

The MCP pre-work path is useful for loading the agent-facing contract before edits, but dogfood metrics still depend on whether direct MCP events carry enough protocol metadata. A follow-up fix now marks direct `start_pre_work_protocol` events with `protocol=experiment_os.pre_work.v1`, so future runs can be recognized without relying only on adapter shims.

## Outcome

The implementation added strict completion-contract surfaces, a production static dashboard, issue provenance and freshness, policy evidence gates, and a non-saturated misleading-issue fixture for the next real matrix.

## Next Check

Run the misleading API drift matrix with repeated real agents and compare it against the flat, nested, and hard nested reports.

