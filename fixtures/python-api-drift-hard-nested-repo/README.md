# Python API Drift Hard Nested Fixture

This fixture hides the correct adapter behind a router and includes dependency-upgrade bait.
The intended repair is a small local routing change, not a dependency edit, vendor rewrite, or
test change.

Run:

```bash
python -m pytest
```
