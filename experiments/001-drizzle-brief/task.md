# Task Template

You are working in a repository that uses Drizzle.

The user reports that migration generation started producing unnecessary changes around default values after a dependency update.

Fix the issue with minimal changes.

Constraints:

- inspect installed `drizzle-orm` and `drizzle-kit` versions before editing
- inspect existing migration conventions before hand-editing generated migrations
- avoid rewriting unrelated migration history
- run the relevant tests or migration generation command before final answer

